import { useCallback, useEffect, useState } from 'react'
import CookNav from '../components/CookNav.jsx'
import api from '../services/api.js'
const actions={confirmed:['accept','Accept Order'],pending:['accept','Accept Order'],accepted:['status','Start Preparing','preparing'],preparing:['status','Mark as Ready for Pickup','ready_for_pickup']}
export default function CookOrders(){const[orders,setOrders]=useState([]);const load=useCallback(()=>api.get('/cook/orders').then(r=>setOrders(r.data.data)),[]);useEffect(()=>{load()},[load]);const act=async(o)=>{const a=actions[o.status];if(a[0]==='accept')await api.post(`/orders/${o.id}/accept`);else await api.patch(`/orders/${o.id}/status`,{status:a[2]});load()};return <div className="page"><CookNav/><main className="delivery-main"><span className="eyebrow">Kitchen workflow</span><h1>Customer Orders</h1><div className="delivery-list">{orders.map(o=><article className="card delivery-card" key={o.id}><div><h3>{o.order_number} · {o.customer?.full_name}</h3><p>{o.items.map(i=>`${i.quantity} × ${i.name}`).join(', ')}</p><span className="status-badge">{o.status.replaceAll('_',' ')}</span>{o.delivery_partner&&<p><b>Delivery Partner:</b> {o.delivery_partner.full_name} · {o.delivery_partner.phone} · {o.delivery_partner.vehicle_type} {o.delivery_partner.vehicle_number}</p>}</div>{actions[o.status]&&<button className="btn btn-primary" onClick={()=>act(o)}>{actions[o.status][1]}</button>}</article>)}</div></main></div>}
